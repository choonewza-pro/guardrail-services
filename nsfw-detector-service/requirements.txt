fastapi>=0.111,<0.120
uvicorn[standard]>=0.30,<0.32
pydantic-settings>=2.5,<3.0
python-multipart>=0.0.9
pillow>=10.4,<11.0
transformers>=4.44,<5.0
timm>=1.0,<2.0
python-dotenv>=1.0
# torch / torchvision are installed separately in the Dockerfile
# via the CUDA-121 index URL for a smaller image.
# For local (non-Docker) runs, uncomment the next two lines:
# torch>=2.3,<2.5
# torchvision>=0.18,<0.20