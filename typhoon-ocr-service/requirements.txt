fastapi>=0.111,<0.120
python-multipart>=0.0.12,<0.1
uvicorn[standard]>=0.30,<0.32
pydantic-settings>=2.5,<3.0
transformers>=4.45,<5.0
accelerate>=0.30,<1.0
pillow>=10.0,<11.0
python-dotenv>=1.0
hf-transfer>=0.1.6
# torch and torchvision are installed separately in the Dockerfile
# (CPU vs CUDA index URL for Docker optimization).
# For local dev without Docker, install torch and torchvision via pip first.
# torch>=2.3.0
# torchvision>=0.18.0
